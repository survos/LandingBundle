
<hr />
<h1>{{ style }}</h1>
{% if clientKeys is defined %}
{% for clientKey in clientKeys %}
<button>
    <a class="btn" href="{{ path('oauth_connect_start', {clientKey: clientKey}) }}"><i class="fab fa-{{clientKey}} fa-3x"></i> Use {{ clientKey|title }}</a>
</button>
{% endfor %}
{% endif %}

{#
<button>
    <a class="btn" href="{{ path('connect_github_start', {clientCode: 'github'}) }}"><i class="fab fa-github fa-3x"></i> Login with Github, using AUTH</a>
</button>

<button>
    <a class="btn" href="{{ path('connect_google_start') }}"><i class="fab fa-google fa-3x"></i> Login with Google</a>
</button>
#}